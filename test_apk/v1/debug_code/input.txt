print("========= TEST CHẠY LUA ĐƠN GIẢN =========")

if _G.SceneData and _G.SceneData.GetAncientBossData then
    local isSatisfy, info = _G.SceneData:GetAncientBossData(16, 20201008)
    
    print("isSatisfy: ", isSatisfy)
    
    if info then
        -- Chỉ in ra vài thuộc tính cơ bản, không dùng hàm dumpTable đệ quy nữa
        print("info.refreshCount: ", info.refreshCount)
        print("info.count: ", info.count)
    else
        print("Biến info trả về nil!")
    end
else
    print("Lỗi: Không tìm thấy _G.SceneData")
end

return "Kết thúc Test!"
