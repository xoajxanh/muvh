MasterSkillTalentTypeEnum = {PS = 1, US = 2}
MasterSkillUpcode = {
  CanUpgrade = enum(0),
  LevelIsFull = enum(),
  TalentNotOpen = enum(),
  DataIsError = enum(),
  RemainPointIsZero = enum(),
  NotMeetPrePoint = enum(),
  NotMeetPreSkill = enum(),
  ProfessionalTalentNotOpen = enum()
}
