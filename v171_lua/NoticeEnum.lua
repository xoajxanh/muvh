NoticeEnum = {
  SYSTEM = enum(1),
  TOP = enum(),
  UPPER = enum(),
  MIDDLE = enum(),
  LINE = enum(),
  MIDDLE_TIP = enum()
}
NoticeStyleEnum = {
  Roll = enum(1),
  Complete = enum(),
  Bomb = enum()
}
