AutoSkillEnum = {
  CommonAttack = enum(1),
  IndSkill = enum(),
  GroupSkill = enum(),
  CommonSkill = enum(),
  BuffSkill = enum(),
  SummonSkill = enum(),
  TreatSkill = enum(),
  SelfSelIndSkill = enum(),
  SelfSelGroupSkill = enum()
}
AutoPickupEnum = {
  SelectAll = enum(1),
  SelectPart = enum()
}
AutoGoTargetEnum = {
  None = enum(0),
  AutoGoTarget = enum()
}
AutoFightStateEnum = {
  Close = enum(0),
  Open = enum(),
  Pause = enum()
}
