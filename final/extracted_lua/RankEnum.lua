RANKTYPE = {
  level = enum(1),
  fight = enum(),
  tower = enum(),
  attack = enum(),
  killMonsterScore = enum(7),
  crossLevel = enum(11),
  crossFight = enum(),
  Crosskalunte = enum(15),
  CrossThree = enum(16)
}
