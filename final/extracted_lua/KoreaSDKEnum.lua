KoreaSDKEnum = {
  airbridgeachieveLevel = enum(9000),
  additional_download_complete = enum(9001),
  login_game = enum(9002),
  character_creation = enum(9003),
  entering_in_game = enum(9004),
  server_choice = enum(9005),
  web_view = enum(10000),
  store_click = enum(10001),
  redstore_click = enum(10002)
}
KoreaSDKNodeEnum = {
  airbridge = enum(1),
  firebase = enum(2)
}
