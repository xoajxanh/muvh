TextAnchor = {
  UpperLeft = enum(0),
  UpperCenter = enum(),
  UpperRight = enum(),
  MiddleLeft = enum(),
  MiddleCenter = enum(),
  MiddleRight = enum(),
  LowerLeft = enum(),
  LowerCenter = enum(),
  LowerRight = enum()
}
ScrollMovementType = {
  Unrestricted = enum(0),
  Elastic = enum(),
  Clamped = enum()
}
