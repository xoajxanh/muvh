BehaviorNodeEnum = {
  BehaviourNode = enum(0),
  DecoratorNode = enum(),
  ConditionNode = enum(),
  ActionNode = enum(),
  SequenceNode = enum(),
  SelectorNode = enum(),
  ParallelNode = enum(),
  IfNode = enum(),
  PickupItemNode = enum(),
  GroupSkillNode = enum(),
  XiFChiSkillNode = enum(),
  IndividualSkillNode = enum(),
  ReturnHomeNode = enum(),
  StrikeBackNode = enum(),
  SpecifyGroupSkillNode = enum(),
  SpecifIndividualSkillNode = enum(),
  SummonSkillNode = enum(),
  UseHpMedNode = enum(),
  UseMpMedNode = enum(),
  FindNearestMonsterNode = enum(),
  FindNearestPlayerNode = enum(),
  MoveNode = enum(),
  MoveSkillNode = enum(),
  ReleasePressSkillNode = enum(),
  BuffSkillNode = enum(),
  PressSkillGroupSkillNode = enum(),
  SetReleaseSkillNode = enum(),
  FindNotBuffTeammateNode = enum(),
  MoveToBuffTeammateNode = enum(),
  AddBuffToTeammate = enum()
}
BehaviorStatusEnum = {
  READY = enum(1),
  RUNNING = enum(),
  SUCCESS = enum(),
  FAILED = enum()
}
