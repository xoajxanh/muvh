GM_DataEnum = {
  All = enum(0),
  NetWork = enum(1),
  Log = enum(2),
  Find = enum(3)
}
GM_CommandEnum = {
  DotInput = enum(0),
  Input = enum()
}
PanelEnum = {
  LogPanel = enum(1),
  CommandPanel = enum()
}
