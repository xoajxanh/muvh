CdEnum = {
  TimeLoopRecoverHp = enum(1),
  TimeLoopRecoverMp = enum(),
  TimeLoopRecoverShield = enum(),
  TimeLoopRecoverAbility = enum(),
  Skill = enum(),
  SkillCommon = enum(),
  Chat = enum(),
  Shop = enum(),
  ItemUse = enum(),
  AFTER_KILL_MONSTER_RECOVER_HP = enum(),
  AFTER_KILL_MONSTER_RECOVER_MP = enum(),
  AFTER_KILL_MONSTER_RECOVER_SHIELD = enum(),
  AFTER_KILL_MONSTER_RECOVER_ABILITY = enum(),
  AFTER_ATTACK_MONSTER_RECOVER_HP = enum(),
  INVITE_MATCH = enum(),
  REDUCE_CD = enum(25),
  SKILL_GROUP = enum(33)
}
