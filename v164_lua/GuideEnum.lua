GuideType = {
  TaskID = enum(1),
  coerceGuide = enum()
}
AutoClickType = {
  No = enum(0),
  Yes = enum(1)
}
OpenGuideEffType = {
  uiShow = enum(1),
  arrivePoint = enum(),
  coinChange = enum(),
  itemChange = enum(),
  taskChange = enum(),
  init = enum()
}
ArrowsType = {
  Up = enum(1),
  Down = enum(),
  Left = enum(),
  Right = enum(),
  RightUp = enum(),
  LeftUp = enum(),
  RightDown = enum(),
  LeftDown = enum()
}
