AutoAcceptTypeInTeamEnum = {
  All = enum(0),
  FriendsOrUnion = enum(),
  Off = enum()
}
TeamActivityType = {
  None = enum(0),
  ColorCastle = enum(),
  DemonSquare = enum()
}
ShowTeamType = {
  MyMemberType = enum(1),
  NearTeamType = enum(),
  nearGuideType = enum(),
  MyFriendsType = enum(),
  MyWarType = enum(),
  MyApplyType = enum()
}
ApplyInviteType = {
  Apply = enum(),
  Invite = enum()
}
