ResourceTypeEnum = {
  Model_Equip = enum(1),
  Model_NPC = enum(),
  Model_Charactor = enum(),
  Model_Mount = enum(),
  Model_Monster = enum(),
  Model_Pet = enum(),
  Model_Gold = enum(),
  Model_Item = enum(),
  Model_InstanceItem = enum(),
  Model_Skill = enum(),
  Effect_Scene = enum(100),
  Effect_Skill = enum(),
  Effect_UI = enum()
}
