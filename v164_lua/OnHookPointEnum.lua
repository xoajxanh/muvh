EOnHookPointType = {
  InComePoint = enum(1)
}
EInComeOnHookPointSubType = {
  GemInComePoint = enum(1),
  ExpInComePoint = enum(),
  EquipInComePoint = enum()
}
OnHookExpType = {
  OnLine = enum(0),
  OutLine = enum()
}
OnHookExpTypeEnum = {
  Rein = enum(1),
  Holy = enum(2)
}
OnHookGetExpEnum = {
  Exp = enum(1),
  CoinExp = enum(2),
  MedicineExp = enum(3)
}
