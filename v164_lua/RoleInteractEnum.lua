RoleOpenType = {
  TeamOpen = enum(0),
  NearTouch = enum(),
  RankOpen = enum(),
  FriendOpen = enum(),
  ChatOpen = enum(),
  WarOpen = enum(),
  Team3v3 = enum()
}
OtherRoleOpenSource = {None = 0, TeamOpen = 1}
RoleInteractFuncEnum = {
  CheckPlayer = enum(1),
  AddFriend = enum(),
  InviteInUnion = enum(),
  PrivateTalk = enum(),
  SendPos = enum(),
  InviteTeam = enum(),
  CreateAndInviteTeam = enum(),
  KickTeam = enum(),
  UpgradeCaptain = enum(),
  LeaveTeam = enum(),
  TeamOwn = enum(),
  ApplyTeam = enum(),
  ApplyUnion = enum(),
  RemoveFriend = enum(),
  MaskFriend = enum(),
  SeleteRole = enum(),
  ReportRole = enum()
}
