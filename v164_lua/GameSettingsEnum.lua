EJoyStickMode = {
  Fixed = enum(1),
  Free = enum()
}
EBattleCamp = {
  None = enum(1),
  All = enum(),
  League = enum(),
  OpLeague = enum(),
  OtherPlayer = enum()
}
EPerformanceQuality = {
  Low = enum(1),
  Middle = enum(),
  High = enum(),
  Auto = enum()
}
EGameSettingsShareType = {
  System = enum(1),
  Role = enum()
}
EGameSettingsPage = {
  Basic = enum(1),
  Display = enum()
}
