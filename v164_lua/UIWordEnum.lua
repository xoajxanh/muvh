UI_Word_AttributeType = {
  attributeUI = enum(),
  equipeUI = enum(),
  equipeExcellenceUI = enum(),
  equipIntensifyUI = enum(),
  AttrChangeUI = enum(),
  GuardTipsUI = enum(),
  equipeTipsUI = enum()
}
